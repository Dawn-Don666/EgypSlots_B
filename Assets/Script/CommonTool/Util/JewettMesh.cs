using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JewettMesh
{
    public static string RanchoAnCup(double a)
    {
        return Math.Round(a, PhysicUnfair.CreepMyxoma).ToString();
    }
    public static string RanchoAnCup(double a, int digits)
    {
        return Math.Round(a, digits).ToString();
    }

    public static double Creep(double a)
    {
        return Math.Round(a, PhysicUnfair.CreepMyxoma);
    }

}
